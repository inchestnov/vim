This repository contains simple .vimrc settings I used for editing single files.  

### Usage
Just clone repository and create symlink to `.vimrc`:

```bash
git clone https://github.com/inchestnov/vim.git
cd vim
ln -s $(pwd)/.vimrc ~/.vimrc
```
